<%= form_with(model: comment_registration_form, method: :post, url: post_comments_path, local: true) do |form| %>
  <% if comment_registration_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comment_registration_form.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% comment_registration_form.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_field :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
